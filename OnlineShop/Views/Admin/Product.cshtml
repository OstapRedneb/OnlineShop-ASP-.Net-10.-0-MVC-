@{
    ViewData["Title"] = "Управление заказами";
}

<div class="admin-layout">
    @await Html.PartialAsync("_AdminSidebar")
    <div class="admin-content">
        <!-- Здесь будет содержимое правой части (ваши формы/таблицы) -->
        <h2>Products</h2>
        <!-- ... -->
    </div>
</div>